backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "products"
    label: "Products"
    file: "products.json"
    fields:
      - { label: "Products", name: "products", widget: "list" }
